CREATE TABLE activities (activity_id INTEGER PRIMARY KEY, name TEXT, event_id INTEGER, startTime DATE, endTime DATE, FOREIGN KEY(event_id) REFERENCES events(event_id));
CREATE TABLE events (event_id INTEGER PRIMARY KEY, name TEXT);
CREATE TABLE users (user_id INTEGER PRIMARY KEY, name TEXT);
